<div class="row">
    <div class="col-10 mx-auto my-3">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div role="alert" class="alert alert-dismissible fade show alert-{{ label }}">
                    {% if label is same as 'success' %}
                        <span class="alert-inner--icon"><i class="fa fa-thumbs-up"></i></span>
                    {% else %}
                        <span class="alert-inner--icon"><i class="fa fa-thumbs-down"></i></span>
                    {% endif %}
                    <span class="alert-inner--text"><strong>{{ label | title }} : </strong>{{ message | raw }}</span>
                </div>
            {% endfor %}
        {% endfor %}
    </div>
</div>

<footer class="footer">

    {# ////////////////////////////////// FIRST PART //////////////////////////////////// #}
    <div class="container border-top">
        <div class="row mt-3">
            <div class="col-12 col-md">
                <h5>TeeShop</h5>
                <small class="d-block text-muted">&copy; {{ "now"|date('Y') }}</small>
            </div>
            <div class="col-6 col-md">
                <h5>Catégories</h5>
                <ul class="list-unstyled">

                </ul>
            </div>
            <div class="col-6 col-md">
                <ul class="list-unstyled">
                    <li><a href="#" class="text-muted">Mentions légales</a></li>
                    <li><a href="#" class="text-muted">Politique de confidentialité</a></li>
                    <li><a href="#" class="text-muted">Contact</a></li>
                </ul>
            </div>
        </div>
    </div>

    {# ////////////////////////////////// SECOND PART //////////////////////////////////// #}
    <div class="container-fluid bg-dark mt-5">
        <p class="text-center text-white p-2 m-0">
            &copy; TeeShop {{ "now"|date('Y') }} - Made with ❤ by
            <a href="https://github.com/BesokDev"
               class="text-reset"
                    {#               data-bs-toggle="tooltip"#}
                    {#               data-bs-placement="right"#}
                    {#               data-bs-html="true"#}
                    {#               title="Have a look to my GitHub <i class='fa-brands fa-github'></i>"#}
            >Besok</a>
        </p>
    </div>
</footer>